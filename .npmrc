
# Allow legacy peer dependencies
legacy-peer-deps=true

# Use exact versions for better reproducibility
save-exact=true

# Set network timeout to be more forgiving
network-timeout=100000

# Use less strict engine check
engine-strict=false
