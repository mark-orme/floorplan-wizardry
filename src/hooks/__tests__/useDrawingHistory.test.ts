
// Stub test for drawing history
import { vi } from 'vitest';
import { Canvas } from 'fabric';

describe('useDrawingHistory', () => {
  test.skip('should handle undo and redo correctly', () => {
    // Skip test until we have the proper testing setup
    const mockCanvas = {} as Canvas;
    expect(mockCanvas).toBeDefined();
  });
});

export {};
