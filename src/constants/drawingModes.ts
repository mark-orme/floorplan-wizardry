
/**
 * Drawing modes used by the canvas tools
 */
export enum DrawingMode {
  SELECT = 'select',
  DRAW = 'draw',
  RECT = 'rectangle',
  CIRCLE = 'circle',
  LINE = 'line',
  PENCIL = 'pencil',
  ERASER = 'eraser',
  TEXT = 'text',
  SHAPE = 'shape'
}
