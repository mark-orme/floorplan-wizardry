
/**
 * Drawing mode constants
 */
export enum DrawingMode {
  SELECT = 'select',
  DRAW = 'draw',
  LINE = 'line',
  RECT = 'rect',
  CIRCLE = 'circle',
  TEXT = 'text',
  ERASER = 'eraser',
  PAN = 'pan',
  MEASURE = 'measure',
  POLYGON = 'polygon',
  WALL = 'wall',
  DOOR = 'door',
  WINDOW = 'window'
}
