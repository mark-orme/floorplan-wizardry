
/**
 * Drawing modes for the canvas
 */
export enum DrawingMode {
  SELECT = 'select',
  DRAW = 'draw',
  STRAIGHT_LINE = 'straight-line',
  WALL = 'wall',
  ROOM = 'room',
  HAND = 'hand',
  ERASER = 'eraser',
  PAN = 'pan',
  LINE = 'line',
  RECTANGLE = 'rectangle',
  CIRCLE = 'circle',
  TEXT = 'text',
  MEASURE = 'measure'
}
