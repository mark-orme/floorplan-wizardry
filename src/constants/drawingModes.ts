
/**
 * Drawing modes for the canvas
 */
export enum DrawingMode {
  SELECT = 'select',
  PAN = 'pan',
  DRAW = 'draw',
  LINE = 'line',
  RECTANGLE = 'rectangle',
  CIRCLE = 'circle',
  TEXT = 'text',
  WALL = 'wall',
  ERASE = 'erase'
}
