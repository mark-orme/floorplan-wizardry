
import React from "react";

// Define GRID_CONSTANTS locally or import from the correct location
const GRID_CONSTANTS = {
  DEFAULT_GRID_SIZE: 50,
  DEFAULT_GRID_COLOR: 'rgba(200, 200, 200, 0.2)',
  DEFAULT_GRID_OPACITY: 0.4,
  MAJOR_GRID_INTERVAL: 5,
  MAJOR_GRID_COLOR: 'rgba(180, 180, 180, 0.5)',
  MAJOR_GRID_OPACITY: 0.6
};

// Fix useCanvas import by importing from the correct location
import { useCanvas } from "@/components/canvas/Canvas";
